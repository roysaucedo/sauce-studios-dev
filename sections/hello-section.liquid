{%- style -%}
  .hello-section-wrapper {
    background-color: #f0f0f0;
    padding: 20px;
    border: 1px solid #ccc;
  }
  .hello-section-title {
    color: blue;
  }
{%- endstyle -%}

<div class="hello-section-wrapper">
  <h3 class="hello-section-title">Event Details</h3>
  {% if product.variants.first.metafields.custom.event_details %}
    <div class="event-details">
      {{ product.variants.first.metafields.custom.event_details }}
    </div>
  {% else %}
    <p>No event details available for the first variant of this product.</p>
  {% endif %}
</div>

{% schema %}
{
  "name": "Event Details",
  "tag": "section"
}
{% endschema %}
